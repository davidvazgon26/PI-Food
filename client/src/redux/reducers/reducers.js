import {TRAER_RECIPES} from '../actions/actions.js'

const initialState ={

}

export default function recipeReducer(state=initialState, action){
    switch (action.type) {
        case TRAER_RECIPES:
            
            break;
    
        default:
            break;
    }
}